# Cache of per-paper citation counts, normally kept in sync with Google Scholar
# by bin/update_scholar_citations.py (requires socials.scholar_userid to be set).
# Empty until a Google Scholar profile ID is configured — see _data/socials.yml.
metadata:
  last_updated:
papers: {}
